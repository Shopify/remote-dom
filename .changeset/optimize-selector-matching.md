---
'@remote-dom/polyfill': patch
---

Optimize selector matching by precomputing HTML element comparison names and avoiding redundant attribute-name normalization and temporary selector-part arrays.
